<tr>
  <td><%= active_session.user_agent %></td>
  <td><%= active_session.ip_address %></td>
  <td><%= active_session.created_at %></td>
  <td><%= link_to 'Sign Out', active_session_path(active_session), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %></td>
</tr>